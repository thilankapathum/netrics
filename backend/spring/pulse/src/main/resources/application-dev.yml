spring:
  datasource:
#    url: jdbc:mysql://localhost:8006/netrics_pulse_db
#    username: root
#    password: root
    url: jdbc:postgresql://localhost:8032/pulse_db
    username: pguser
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
#    database: mysql
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

    logging:
      level:
        org.springframework.cloud.gateway: DEBUG
