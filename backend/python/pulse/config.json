{
  "database": {
    "host": "${POSTGRES_HOST}",
    "port": "${POSTGRES_PORT}",
    "database": "${POSTGRES_DB_PULSE}",
    "user": "${POSTGRES_USER}",
    "password": "${POSTGRES_PASSWORD}"
  },
  "base_folders": {
    "day-average": "/app/day-average",
    "busy-hour": "/app/busy-hour"
  },
  "clear_redis_cache_url": "http://pulse:8012/api/v1/pulse/cache/evict-rat-granularity",
  "dedup_latest_oss_url": "http://pulse:8012/api/v1/pulse/kpi-day/dedup-oss"
}