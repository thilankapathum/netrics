{
  "database": {
    "host": "${POSTGRES_HOST}",
    "port": "${POSTGRES_PORT}",
    "database": "${POSTGRES_DB_PULSE}",
    "user": "${POSTGRES_USER}",
    "password": "${POSTGRES_PASSWORD}"
  },
  "base_folder": "/app/day-average",
  "clear_redis_cache_url": "http://pulse:8012/api/v1/pulse/cache/evict-and-warmup"
}